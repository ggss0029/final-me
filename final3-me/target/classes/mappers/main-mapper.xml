<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="mainMapper">
	<resultMap type="board" id="mainResultset">
		<result column="BOARD_NO" property="boardNo"/>
		<result column="BOARD_TITLE" property="boardTitle"/>
		<result column="USER_NAME" property="boardWriter"/>
		<result column="CATEGORY_CODE" property="categoryCode"/>
		<result column="CREATE_DATE" property="createDate"/>
		<result column="COUNT" property="count"/>
		<result column="STATUS" property="status"/>
	</resultMap>
	
	
	<select id="mainNoticeList" resultMap="mainResultset">
		SELECT BOARD_NO, BOARD_TITLE, USER_NAME, CREATE_DATE, COUNT
		FROM BOARD B
		JOIN MEMBER M ON (BOARD_WRITER = USER_NO)
		WHERE B.STATUS = 'Y'
		AND CATEGORY_CODE = 'B1'
		ORDER BY BOARD_NO DESC
	</select>
	
</mapper>